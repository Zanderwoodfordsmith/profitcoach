/** Campaign voice/video uploads. Keep in sync with messaging-attachments file_size_limit. */
export const MAX_CAMPAIGN_STEP_MEDIA_BYTES = 50 * 1024 * 1024;
export const MAX_CAMPAIGN_STEP_MEDIA_MB = 50;

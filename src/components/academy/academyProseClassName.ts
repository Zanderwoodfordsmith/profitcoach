/**
 * Shared typography for lesson body content (preview, coach view, and visual
 * editor). The rules live in `src/app/globals.css` under `.academy-prose`,
 * because markdown and pasted Google Docs HTML arrive without class names.
 */
export const academyProseClassName = "academy-prose";

/** Long-form variant for the Guide tab: larger type, clearer section breaks. */
export const academyGuideProseClassName = "academy-prose academy-prose--guide";

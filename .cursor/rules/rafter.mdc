---
description: "On-demand Rafter router. Only invoke when the user asks for a Rafter scan, security review, audit, or command-risk check — not on every feature or before marking work done."
alwaysApply: false
---

# rafter — security toolkit router

Pointer rule. Use the `rafter` skill (full guidance at `.claude/skills/rafter/SKILL.md`) **only when the user asks** for a scan, security review, audit, or command-risk check.

Do not auto-run `rafter run`, `rafter secrets`, or the rafter sub-agent after implementing a feature.

- Run `rafter run` for the default tier — remote SAST + SCA + secrets. Needs `RAFTER_API_KEY`.
- Run `rafter run --mode plus` for agentic deep-dive when the user wants that.
- Run `rafter secrets <path>` for offline secrets-only (NOT a code security scan).
- Run `rafter agent exec --dry-run -- <cmd>` to classify a shell command's risk when the user asks.

version: "1"

# Profit Coach — Rafter project policy.
# Scans are on-demand (user asks). Not part of the agent implement loop.
risk_level: moderate

command_policy:
  mode: approve-dangerous

scan:
  plus_requires_approval: false
  exclude_paths:
    - "Import/"
    - "reports/"
    - ".ica-research/"
    - "actors/**/storage/"

docs:
  - id: rafter-cse
    description: Code Security Engine — run only when asked
    path: .cursor/rules/rafter-cse.mdc
    tags: [threat-model, secure-coding]
